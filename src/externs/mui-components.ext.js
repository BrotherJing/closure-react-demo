var MuiThemeProviderProps = {
  theme,
  sheetsManager,
  disableStylesGeneration,
  children,
}

var Select = {
  inputProps,
}

var ThemeOptions = {
  shape,
  breakpoints,
  direction,
  mixins,
  overrides,
  palette,
  props,
  shadows,
  spacing,
  transitions,
  typography,
  zIndex,
}

var Theme = {
  shape,
  breakpoints,
  direction,
  mixins,
  overrides,
  palette,
  props,
  shadows,
  spacing,
  transitions,
  typography,
  zIndex,
}

var FontStyleOptions = {
  htmlFontSize,
  allVariants,
  useNextVariants,
}

var StyledComponentProps = {
  classes,
  innerRef,
}

var GridProps = {
  alignContent,
  alignItems,
  component,
  container,
  direction,
  item,
  justify,
  spacing,
  wrap,
  zeroMinWidth,
}
